#ifndef PILHA_H_INCLUDED
#define PILHA_H_INCLUDED
#include <stdlib.h>
#include <stdbool.h>
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
typedef struct TipoPilha
{
  int item;
  struct TipoPilha *next,*prev;
}TipoPilha;
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
bool procura(TipoPilha *pilha1, int num,bool res)
{
  while(pilha1->prev!=NULL){pilha1=pilha1->prev;};
  while(num!=pilha1->item || pilha1->next!=NULL)
      {
        if(pilha1->next==NULL && pilha1->item!=num){res=false; return;}
        if(pilha1->item==num){res=true; return;}
        else{pilha1=pilha1->next;}
      };  
}
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
TipoPilha empilha(TipoPilha *pilha2, int num)
{
    while(pilha2->next!=NULL){pilha2=pilha2->next;};
    if(pilha2->next==NULL)
        {
            pilha2->next=(TipoPilha*)malloc(sizeof(TipoPilha));
            if(pilha2->next==NULL){
            printf("erro ao empilhar");
            free(pilha2); exit(0);
                                  }
            pilha2->next->item=num;
            pilha2->next->prev=pilha2;
            pilha2->next->next=NULL;
        }
}
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
TipoPilha desempilha(TipoPilha *pilha3)
{
    if(pilha3->next->next==NULL)
    {
        pilha3->next->prev=NULL;
        pilha3->next=NULL;
    }
    else {desempilha(pilha3->next);}
}
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
void mostrapilha(TipoPilha *pilha4)
{
    do{pilha4=pilha4->next;}while(pilha4->next!=NULL);
    do{
        printf("%d\n",pilha4->item);
        pilha4=pilha4->prev;
      }while(pilha4->prev!=NULL);
}
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#endif // PILHA_H
